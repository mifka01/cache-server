[pytest]
testpaths = tests
addopts = -ra
markers =
    contract: stable external API contract tests
    scenario: end-to-end workflow tests
    nonfunctional: performance/load/resilience tests
    smoke: fast checks for local feedback
    requires_cachix: needs cachix binary configured for target server
    requires_token: needs CACHIX_AUTH_TOKEN available
